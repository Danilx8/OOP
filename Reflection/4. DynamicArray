В конструкторе указал, что будет создан динамический массив, без упоминания, что он будет пуст
Не описал методы put_right и put_left (put считаю синонимичным insert)
В качестве отдельного метода указал Resize, чтобы по-разному реализовывать методы расширения и сужения динамического 
массива. Вероятно, в эталонном решении такого метода нет для гибкости реализации